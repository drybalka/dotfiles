Match host * exec "gpg-connect-agent UPDATESTARTUPTTY /bye"

IdentitiesOnly yes
# Provide identities to ssh explicitly by using
# `ssh -i ~/.ssh/your_identity.pub`
# Create the IdentityFile with the public ssh key for each identity by running
# `gpg --export-ssh-key your_identity > ~/.ssh/your_identity.pub`
